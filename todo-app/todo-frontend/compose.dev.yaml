services:
  app:
    container_name: hello-front-dev
    image: hello-front-dev
    build:
      context: . 
      dockerfile: dev.Dockerfile 
    volumes:
      - ./:/usr/src/app
    ports:
      - 5173:5173
